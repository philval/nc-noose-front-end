import React from 'react'

const ArticleAddButton = ({ handleArticleAddButton }) => {
  return (
    <button onClick={handleArticleAddButton}>New Article</button>
  )
}

export default ArticleAddButton